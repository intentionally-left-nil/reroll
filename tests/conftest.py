"""Shared pytest fixtures.

Currently empty on purpose: reroll has no implementation yet. Add fixtures
here as they're needed by more than one test module (e.g. a `metadata_dir`
tmp-path builder for hand-crafted METADATA content).
"""
